services:
  postgres:
    image: postgres:16
    container_name: irctc-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: irctc
      POSTGRES_USER: irctc
      POSTGRES_PASSWORD: irctc_dev_password

    ports:
      - "5432:5432"

    volumes:
      - irctc_postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U irctc -d irctc"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  irctc_postgres_data: